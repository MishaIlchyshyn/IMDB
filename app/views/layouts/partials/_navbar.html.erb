<nav class="navbar navbar-expand-md navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to "IMDB", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <% if current_page?("/admins/sign_in") %>
      <span></span>
    <% else %>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item <%= active_if?('movies', 'index') %>">
            <%= link_to t('home.nabvar.movies'), movies_path, class: "nav-link" %>
          </li>
        </ul>
        <ul class="navbar-nav">
          <% if user_signed_in? %>
            <li class="nav-item">
              <span class="navbar text-purple-300"><%= current_user.email %></span>
            </li>
            <li class="nav-item">
              <%= link_to t('home.navbar.output'), destroy_user_session_path, class: "nav-link", :method => :delete %>
            </li>
          <% else %>
            <li class="nav-item <%= active_if?('sessions', 'new') %>">
              <%= link_to t('home.navbar.login'), new_user_session_path, class: "nav-link" %>
            </li>
            <li class="nav-item <%= active_if?('registrations', 'new') %>">
              <%= link_to t('home.navbar.register'), new_user_registration_path, class: "nav-link" %>
            </li>
          <% end %>
        </ul>
      </div>
    <% end %>
  </div>
</nav>